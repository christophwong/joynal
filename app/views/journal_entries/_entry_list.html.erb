<div class="journal-list-show">
  <% @journal_entries.each do |entry| %>
  <div class="entry">
    <%= link_to "#{entry.content[0..35]} .... ##{entry.tags.map(&:name).join(" #")}", entry, :class => 'user-entry', remote: true %>
  </div>
  <% end %>
</div>

