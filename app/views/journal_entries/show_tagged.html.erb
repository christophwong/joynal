<div class="container">
  <h1>Journal entries with the tag '<%= @tag_name %>'</h1>

  <ul>
    <% @tagged_entries.each do |entry| %>
      <% d = Date.parse(entry.created_at.to_s) %>
      <li><%= link_to d.strftime('%a %b %d %Y'), journal_entry_path(entry) %></li>
    <% end %>
  </ul>

</div>
